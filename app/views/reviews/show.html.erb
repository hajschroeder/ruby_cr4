<h1>Reviewer: <%= @review.author %></h1>
<h2>Product: <%= @product.name %></h2>
<h2>Rating: <%= @review.rating %></h2>
<h3><%= @review.content_body %></h3>

<p><%= link_to "Edit", edit_product_review_path(@product, @review) %></p>
<p><%= link_to "Delete", product_review_path(@product, @review), :data => {:confirm => "Are you sure? Once it is gone it is gone", :method => "delete" } %></p>
<p><%= link_to "Return to product", products_path(@product) %></p>